services:
  - type: web
    name: plantpulse-api
    env: docker
    plan: free
    rootDir: apps/api
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ENVIRONMENT
        value: production
      # Add other necessary env variables below in the Render Dashboard:
      # - FIREBASE_CREDENTIALS
      # - FIREBASE_STORAGE_BUCKET
      # - SENTRY_DSN
